config {
    type: "table",
    name:"revenue on the asked date"
}

SELECT
  LISTAGG(order_id) as Order_IDs,
  SUM(total_bill) AS total_revenue
FROM
  presentation_dataset.orders
WHERE
  order_date='${dataform.projectConfig.vars.orderdate}'
