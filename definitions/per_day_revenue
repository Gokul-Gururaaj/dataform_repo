config 
{ 
  type: "table"
  name: "particular_day_revenue" 
}

select sum(total_bill) as total_revenue from presentation_dataset.orders where order_date='dataform.projectConfig.vars.orderdate'; 
